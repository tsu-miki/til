# CLAUDE.md

## 作業の進め方（必ず守る）

TIL を追加・修正するときは、次の順で進める。

1. **まずチャットに全文を出力する。** 追加・変更する Markdown の本文をそのままチャットに出し、確認を得る。
2. **確認が取れてからファイルに書き込む。** 先にコミットしない。
3. **`./scripts/build-index.sh` を実行して README の目次を更新する。**
4. **コミットする。** メッセージは `トピック: 学びの要約` の形式（例: `k8s: rollout restart で Pod を作り直す`）。
5. **PR を作る。**

チャットへの出力を飛ばして、いきなりファイル作成・コミットまで進めないこと。

## 書き方の規約

README.md の「書き方」に従う。

- 1 ファイル 1 学び。400 字以内。
- 構成は「タイトル → 要約 1 文 → 具体例（コード）→ 補足 → 参照リンク」。
- ファイル名は `トピック/kebab-case.md`。
- 未検証の推測は書かない。確認できた事実だけを残す。
- 所要 10〜15 分を超えるものは TIL にしない。
- 雛形は `templates/til.md`。

## 目次

README.md の `<!-- index:begin -->` 〜 `<!-- index:end -->` は `scripts/build-index.sh` の生成物。手で編集しない。
